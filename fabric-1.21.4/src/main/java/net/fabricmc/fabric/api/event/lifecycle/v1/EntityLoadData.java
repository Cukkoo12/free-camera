package net.fabricmc.fabric.api.event.lifecycle.v1;

/**
 * Stub — Fabric Loom instruments the Minecraft {@code Entity} class to
 * implement this interface. The class file must be present on the classpath
 * at compile time. This stub provides it without depending on a specific
 * version of {@code fabric-lifecycle-events-v1}.
 * <p>
 * The real interface lives in Fabric API's lifecycle-events module and has
 * the same marker-only signature.
 */
public interface EntityLoadData {
    // Marker interface — no methods
}
